version: '3.1'

services:
  dev:
    container_name: mstaff-micro__front-symfo-1__dev__symfo
    build: docker/dev
    working_dir: /application
    ports:
      - '21000:8000'
    volumes:
      - .:/application
    networks:
      - mstaff-micro.network

networks:
  mstaff-micro.network:
    external: true